<Window x:Class="WPFTutorial.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFTutorial"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <!--<Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Yellow"/>
            <Setter Property="Foreground" Value="Black" />
        </Style>

        <Style TargetType="Button" x:Key="DarkButtonStyle">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="Blue" />
        </Style>

        <Style TargetType="Button" x:Key="LightButtonStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Blue" />
        </Style>
    </Window.Resources>-->
    <Grid>
        <StackPanel>
            <Button Content="Base Button"/>
            <Button Style="{StaticResource DarkButtonStyle}" Content="Dark Button"/>
            <Button Style="{StaticResource LightButtonStyle}" Content="Light Button"/>
        </StackPanel>
    </Grid>
</Window>
