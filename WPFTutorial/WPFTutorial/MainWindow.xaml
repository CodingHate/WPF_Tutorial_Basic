<Window x:Class="WPFTutorial.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFTutorial"
        mc:Ignorable="d"
        Title="MainWindow" Height="150" Width="450">
    <Grid Background="{Binding Background.Color}">
        <DockPanel LastChildFill="False">
            <StackPanel Width="150" DockPanel.Dock="Top">
                <TextBlock Text="{Binding Person.Name, StringFormat=Welcom {0}}"/>
                <TextBox Text="{Binding Person.Name, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Submit"/>
            </StackPanel>
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" DockPanel.Dock="Bottom">
                <Button  x:Name="Red" Content="Red" Click="Red_Click"/>
                <Button x:Name="Blue" Content="Blue" Click="Blue_Click"/>
                <Button x:Name="Yellow" Content="Yellow" Click="Yellow_Click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
